{
  "private": true,
  "scripts": {
    "lint": "pnpm mega-linter-runner -f cupcake",
    "prepare": "husky",
    "validate:manifest": "pnpx ajv-cli validate -s data/v2/manifest.schema.json -d data/v2/manifest.json"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "mega-linter-runner": "^9.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
