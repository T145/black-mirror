{
  "name": "black-mirror",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "lint": "node scripts/lint.js",
    "prepare": "husky"
  },
  "devDependencies": {
    "ajv-cli": "^5.0.0",
    "husky": "^9.1.7",
    "mega-linter-runner": "^9.3.0"
  },
  "engines": {
    "node": ">=20"
  }
}
